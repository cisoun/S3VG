var rouge = "#df421e";

setPage(500, 500, WHITE);

fillColor(rouge);
rect(20, 20, 100, 100, 0);

strokeWidth(1);
strokeColor(YELLOW);
for i = 0 to 9 {
	print("étape " + i);
	strokeWidth(i+1);
	line(0, i*11, 100, i*11);
}
