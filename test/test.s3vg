var rouge = "#df421e";

setPage(500, 500, "#fff");

fillColor(rouge);
rect(0, 0, 100, 100);

strokeWidth(1);
strokeColor("#330f07");
for i = 0 to 9 {
	print("étape " + i);
	strokeWidth(i+1);
	line(0, i*11, 100, i*11);
}
